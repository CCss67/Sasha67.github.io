# 🌊 漂流瓶 · 海

一个纯前端、零依赖的交互式心情漂流瓶原型。

把心情写进瓶子，扔进海里。它会穿过台湾海峡、横跨太平洋、绕过新西兰，最后被洋流带回海岸。

点击海鸥喂食可以加快时间流速。每个瓶子都会记录自己的航行轨迹和返航时间。

---

## 目录

- [项目简介](#项目简介)
- [在线体验](#在线体验)
- [快速开始](#快速开始)
- [核心玩法](#核心玩法)
- [视觉设计](#视觉设计)
- [技术实现](#技术实现)
- [文件结构](#文件结构)
- [如何自定义](#如何自定义)
- [部署到 GitHub Pages](#部署到-github-pages)
- [常见问题](#常见问题)
- [版本说明](#版本说明)
- [迭代方向](#迭代方向)
- [许可证](#许可证)

---

## 项目简介

**漂流瓶 · 海** 是一个以「心情释放」为核心体验的前端原型。

用户可以把不想对别人说的话写进瓶子，扔进海里。瓶子会随着洋流漂向远方，几天后又被海水带回沙滩。整个过程中，用户只需要偶尔回来看看它到哪了。

### 设计意图

| 传统日记应用 | 漂流瓶 · 海 |
|--------------|-------------|
| 记录、存档、回顾 | 释放、漂流、忘记 |
| 强调"你写下了什么" | 强调"你放下了什么" |
| 需要主动打开 | 被动地被海浪带回 |
| 数据越积越多 | 瓶子会自然消失 |

它不是效率工具，是一个**情绪容器**。

---

## 在线体验

https://你的用户名.github.io/infj-sea/

---

## 快速开始

1. 保存代码为 `index.html`
2. 双击用浏览器打开（推荐 Chrome / Edge / Safari）
3. 点击屏幕中央的 🐚 贝壳 → 写下心情
4. 可选勾选「让它在公共海滩出现」
5. 点「放进海里」
6. 点击天上的 🕊️ 海鸥喂食 → 心情值上升 → 时间流速加快

不需要安装、不需要联网、不需要注册。

---

## 核心玩法

### 1. 写一封信

- 点击 🐚 贝壳打开写信弹窗
- 写下任何想说的话
- 可选择「公开到公共海滩」，让其他人看到（原型中为本地模拟）
- 点「放进海里」

### 2. 观察瓶子漂流

- 瓶子入海后，会显示在海面区域
- 每个瓶子标注已航行的天数（如 `3d`）
- 瓶子会随海浪上下浮动

### 3. 喂食海鸥，加快时间

- 点击天上的 🕊️ 海鸥
- 每次喂食 **心情值 +1**（最高 5 点）
- 心情值越高，**时间流速越快**

| 心情值 | 时间流速 |
|--------|----------|
| 0 | 1.0x |
| 1 | 1.2x |
| 2 | 1.5x |
| 3 | 2.0x |
| 4 | 2.5x |
| 5 | 3.0x |

### 4. 等待瓶子返航

- 瓶子在海上漂流，会经过 10 个航行阶段：

| 天数 | 阶段 |
|------|------|
| 0-0.5 天 | 🌊 刚刚放进海里，还在近岸漂流 |
| 0.5-2 天 | 🌅 穿过晨雾，正驶向台湾海峡 |
| 2-5 天 | 🐬 遇到了一群海豚，它们在船头带路 |
| 5-9 天 | ☀️ 到达菲律宾海域，海水温暖又清澈 |
| 9-15 天 | ✨ 正在横跨太平洋，夜里的星星特别亮 |
| 15-25 天 | 🏔️ 接近新西兰南岛，远处的雪山若隐若现 |
| 25-40 天 | 🌪️ 在太平洋深处遇到了一场风暴，但安然无恙 |
| 40-60 天 | 🌏 已经绕了地球半圈，正在穿越印度洋 |
| 60-90 天 | 🏠 正在返航的途中，海风带着家的味道 |
| 90+ 天 | 💫 完成了环球旅行，正在被洋流带回海岸 |

- 心情值越高，**返航概率越高**
- 返航的瓶子会出现在沙滩上，标注返航日期
- 点击返航的瓶子，可以看到它的完整旅程

### 5. 公共海滩

- 点击右下角 ⛵ 小船进入公共海滩
- 看到所有被标记为「公开」的瓶子
- 原型中为本地模拟，所有数据都在你自己的浏览器里

### 6. 测试面板

- 点击右上角 `· · ·` 打开测试面板
- 可手动增加心情、触发返航、快进时间、重置数据
- 用于体验完整的漂流流程

---

## 视觉设计

### 场景元素

| 元素 | 说明 |
|------|------|
| 🌊 海天背景 | 从浅蓝到深蓝的渐变，从上到下 |
| 🌊 海浪 | 三层错位漂移的透明波浪，缓慢流动 |
| 🏖️ 沙滩 | 底部 35%，米黄色渐变 |
| 🐚 沙滩装饰 | 贝壳、珊瑚、脚印散落 |
| 🕊️ 海鸥 | 5 只缓慢飞行的海鸥，可点击喂食 |
| 🍾 漂流瓶 | 海面上漂浮的瓶子，可点击查看详情 |
| ⛵ 小船 | 右下角入口，进入公共海滩 |
| 🐚 贝壳 | 屏幕中央，点击打开写信弹窗 |
| 🐾 心情值 | 右下角 5 个爪印，显示当前心情 |

### 色彩

| 用途 | 颜色 |
|------|------|
| 深海 | `#0f2840` |
| 海水 | `#1a3a55` |
| 沙滩 | `#fceecb` |
| 瓶子（新） | 淡蓝光晕 |
| 瓶子（旧） | 棕褐色 + 模糊 |

### 氛围设计原则

1. **低对比度**：所有文字都是半透明，不抢视觉焦点
2. **慢节奏**：所有动画都是 4-20 秒的缓慢循环
3. **安静感**：没有刺眼的按钮、没有强提示
4. **留白**：大量空间留给海浪和空无一物的海面

---

## 技术实现

### 技术栈

- 纯 HTML + CSS + 原生 JavaScript
- 无框架、无构建、无依赖
- 单文件，离线可用
- 数据存 `localStorage`

### 核心模块

```
1. 数据层（state）
   - bottles[]：瓶子列表
   - nextId：下一个瓶子 ID
   - simulatedDays：模拟天数（测试用）
   - mood：心情值（0-5）
   - lastReturnCheck：上次返航检查时间

2. 时间系统
   - getSpeedMultiplier()：根据心情返回时间倍率
   - getBottleDays()：计算瓶子已航行天数
   - getJourneyStage()：根据天数返回当前阶段

3. 返航系统
   - checkReturn()：检查是否触发返航
   - 概率 = 基础 25% + 心情 × 4%（上限 60%）
   - 每次返航随机选择 10%-60% 的航行中瓶子

4. 渲染系统
   - renderSeaBottles()：海面瓶子
   - renderReturnedBottles()：沙滩上返航的瓶子
   - renderPublic()：公共海滩
   - renderMood()：心情值显示

5. 交互系统
   - 海鸥点击 → 心情 +1
   - 贝壳点击 → 打开写信弹窗
   - 小船点击 → 进入公共海滩
   - 瓶子点击 → 查看详情
```

### 心情值与时间流速

```js
const SPEED_MULTIPLIERS = [1, 1.2, 1.5, 2.0, 2.5, 3.0];

function getSpeedMultiplier() {
  const idx = Math.min(state.mood, 5);
  return SPEED_MULTIPLIERS[idx] || 1;
}
```

### 航行阶段

```js
const JOURNEY_STAGES = [
  { minDay: 0, maxDay: 0.5, emoji: '🌊', desc: '刚刚放进海里…' },
  // ... 10 个阶段
];

function getJourneyStage(days) {
  for (const s of JOURNEY_STAGES) {
    if (days >= s.minDay && days < s.maxDay) return s;
  }
  return JOURNEY_STAGES[JOURNEY_STAGES.length - 1];
}
```

### 自动循环

```js
function autoLoop() {
  const speed = getSpeedMultiplier();
  const interval = Math.max(2000, 8000 / speed);
  checkReturn();
  renderAll();
  setTimeout(autoLoop, interval);
}
```

心情越高，循环越快，返航检查越频繁。

### 数据存储

```js
// localStorage key
'drift_bottles_v7'

// 数据结构
{
  bottles: [
    {
      id: 1,
      text: '今天有点累，没什么原因',
      date: '2026-09-16T10:00:00.000Z',
      returned: false,
      shared: true,
      returnDate: null
    }
  ],
  nextId: 2,
  simulatedDays: 0,
  mood: 0,
  lastReturnCheck: null
}
```

### 敏感词过滤

内置简单敏感词列表，命中则拒绝投放：

```js
const SENSITIVE_WORDS = ['暴力', '色情', '政治', '辱骂', '毒品', '犯罪', '仇恨'];

function containsSensitive(t) {
  for (const w of SENSITIVE_WORDS) {
    if (t.includes(w)) return true;
  }
  return false;
}
```

---

## 文件结构

```
.
├── index.html       # 单文件应用
└── README.md        # 本文档
```

---

## 如何自定义

### 修改航行阶段

在 `JOURNEY_STAGES` 数组里修改：

```js
const JOURNEY_STAGES = [
  { minDay: 0, maxDay: 0.5, emoji: '🌊', desc: '你的文案' },
  // ...
];
```

### 修改时间流速

在 `SPEED_MULTIPLIERS` 数组里修改：

```js
const SPEED_MULTIPLIERS = [1, 1.2, 1.5, 2.0, 2.5, 3.0];
```

### 修改返航概率

在 `checkReturn()` 里修改：

```js
const baseChance = 0.25;       // 基础触发率
const moodBonus = state.mood * 0.04;  // 心情加成
const baseRate = 0.10 + Math.random() * 0.20;  // 每次返航比例
const moodRate = state.mood * 0.03;
```

### 修改敏感词

在 `SENSITIVE_WORDS` 数组里增删：

```js
const SENSITIVE_WORDS = ['暴力', '色情', '政治', ...];
```

### 修改主题色

主色是深蓝 `#0f2840` 和海水色 `#1a3a55`，全局替换即可：

```bash
sed -i '' 's/#0f2840/#1a1a2e/g' index.html
sed -i '' 's/#1a3a55/#2d2d44/g' index.html
```

### 修改海鸥数量

在 `.seagull-group` 里增删 `.seagull-interactive`：

```html
<div class="seagull-interactive" data-index="5">
    🕊️
    <span class="label-hint">· 新海鸥 ·</span>
</div>
```

并在 CSS 里加对应的 `:nth-child(6)` 定位。

---

## 部署到 GitHub Pages

### 1. 文件准备

```
infj-sea/
├── index.html       # 把 infj-sea.html 改名为 index.html
└── README.md
```

### 2. 上传到 GitHub

1. 新建仓库 `infj-sea`（Public）
2. `Add file` → `Upload files` → 拖入所有文件
3. `Commit changes`

### 3. 开启 Pages

1. 仓库 → `Settings` → `Pages`
2. Source 选 `Deploy from a branch`
3. Branch 选 `main`，目录选 `/ (root)`
4. 点 `Save`，等 1~2 分钟
5. 访问 `https://你的用户名.github.io/infj-sea/`

---

## 常见问题

### Q1：瓶子会真的被其他人看到吗

当前版本为**纯前端原型**，所有数据都存在你自己的浏览器里。
「公共海滩」显示的是你自己标记为公开的瓶子，不是其他用户的。

真正的多人共享需要后端服务，见 v2 迭代方向。

### Q2：数据会丢失吗

数据存在 `localStorage`，以下情况会丢失：

- 清除浏览器数据
- 使用无痕模式
- 换浏览器或设备

如果需要持久保存，等 v2 接入后端。

### Q3：瓶子会真的消失吗

不会。返航的瓶子会一直留在沙滩上，直到你手动重置数据。

### Q4：心情值会下降吗

不会。心情值只会上升，最高 5 点。想重新开始可以点测试面板的「重置数据」。

### Q5：为什么我的瓶子航行了 100 天还没返航

返航是**概率事件**，不是必然事件。心情值越高，返航概率越高。

可以点击测试面板的「手动返航」强制触发。

### Q6：支持多设备同步吗

不支持。数据只存在当前浏览器。

### Q7：能改成中文界面吗

本身就是中文。如果需要英文版，全文替换文案即可。

---

## 版本说明

### v1：纯交互原型（当前版本）

**核心特征**：

- 单文件、零依赖、离线可用
- 所有数据存 `localStorage`
- 航行和返航是本地模拟
- 公共海滩是本地假数据
- 内容过滤是简单关键词匹配

**适用场景**：

- 体验产品形态
- 验证情绪容器这个概念
- 作为个人小工具使用

**不适用场景**：

- 真正的多人共享漂流瓶
- 跨设备同步
- 内容智能生成

---

## 迭代方向

### v2：接入 AI

**目标**：从「本地模拟」升级为「真实漂流 + 智能匹配」。

#### 方向 1：AI 生成漂流瓶内容

**现状**：瓶子里的内容只能由用户手写。

**v2 方案**：

- 用户可以只写一个关键词（如「工作」「失恋」「迷茫」）
- AI 生成一段温柔的、不说教的、可投放的漂流瓶内容
- 用户可以选择「用 AI 版」「自己写」「混合编辑」

**技术路径**：

```js
async function generateBottleContent(keyword, mood) {
  const prompt = `
    用户此刻的心情关键词是「${keyword}」。
    请生成一段 20-50 字的漂流瓶内容。
    要求：
    1. 语气温柔、克制、不说教
    2. 不要用「加油」「你可以的」等套话
    3. 像一个人在深夜写下的句子
    4. 可以是疑问、独白、或只是陈述
  `;
  return await callLLM(prompt);
}
```

**设计意图**：

- 降低写作门槛
- 让内容更真实、更值得漂流
- 用户只需要表达情绪，AI 负责把情绪变成句子

#### 方向 2：智能匹配送瓶对象

**现状**：瓶子只在自己浏览器里漂流，不会真的被其他人收到。

**v2 方案**：

- 接入后端服务，实现真实的漂流瓶投递
- 用 AI 做**内容与接收者的语义匹配**
- 例如：
  - 一个写「工作压力大」的瓶子，优先送给最近也写过「工作」的人
  - 一个写「想念小时候」的瓶子，优先送给写过「童年」的人
  - 一个写「希望她一切都好」的瓶子，优先送给写过「思念」的人

**匹配逻辑**：

```js
async function matchBottleToUser(bottle, userPool) {
  // 1. 用 embedding 把瓶子内容转成向量
  // 2. 用 embedding 把每个用户的历史内容转成向量
  // 3. 计算余弦相似度
  // 4. 返回相似度最高且最近没收到过瓶子的用户
  // 5. 用 AI 做二次筛选（避免匹配到内容相似的痛苦经历）
}
```

**设计意图**：

- 让漂流瓶真正「漂」起来
- 让内容找到真正需要它的人
- 避免随机投递带来的噪音

#### 方向 3：AI 生成返航寄语

**现状**：瓶子返航后只显示文字和返航日期。

**v2 方案**：

- 瓶子返航时，AI 根据瓶子的内容和航行天数，生成一段「海的话」
- 例如：
  - 航行 3 天返航：「它在近岸转了一圈，没有走远，可能它知道你还没准备好让它离开。」
  - 航行 90 天返航：「它绕了地球一圈。你写下的那句话，已经比你先看过了这个世界。」
  - 航行 200 天返航：「它在海上待了很久。久到你已经忘了写过什么，但海还记得。」

**设计意图**：

- 让返航变成一个有仪式感的时刻
- 让用户感受到「有人在回应」
- 增强情绪容器的闭环

#### 方向 4：AI 内容安全

**现状**：敏感词过滤是简单关键词匹配，容易误伤也容易漏。

**v2 方案**：

- 接入 AI 内容审核 API
- 识别更复杂的情感（如自杀倾向、自伤倾向）
- 对高危内容做温柔干预，而非直接拒绝

### v2 的架构调整

```
v1（当前）：
  用户 → 本地 localStorage → 本地渲染

v2（规划）：
  用户 → 前端
        ↓
    后端 API（Node / Cloudflare Workers）
        ↓
    ┌───────────┼───────────┐
    ↓           ↓           ↓
  数据库      AI 服务     内容审核
  (Postgres)  (LLM API)   (审核 API)
```

### v2 的技术选型建议

| 模块 | 推荐 |
|------|------|
| 前端 | 保留单文件 HTML（无需构建） |
| 后端 | Cloudflare Workers / Vercel Functions |
| 数据库 | Supabase / Cloudflare D1 |
| LLM | DeepSeek / OpenAI / 智谱 |
| Embedding | OpenAI text-embedding-3-small |
| 内容审核 | 各 LLM 服务商自带 |
| 认证 | 匿名 ID + localStorage token |

### v2 的优先级

| 优先级 | 功能 | 说明 |
|--------|------|------|
| P0 | AI 生成漂流瓶内容 | 降低写作门槛 |
| P0 | 真实漂流瓶投递 | 从本地模拟升级为多人共享 |
| P1 | 智能匹配 | 让内容找到对的人 |
| P1 | 内容安全 | 保护用户 |
| P2 | AI 返航寄语 | 增强仪式感 |
| P2 | 跨设备同步 | 用户系统 |
| P3 | 匿名社区 | 公共海滩的真实版本 |
| P3 | 移动端 App | 原生体验 |

### v2 的设计原则（延续 v1）

无论 v2 加入多少 AI 能力，以下原则不变：

1. **不制造焦虑**：不推送、不提醒、不计分
2. **不索取注意力**：用户离开后不需要想着回来
3. **不评判**：AI 不分析用户情绪，只负责表达和匹配
4. **可忘记**：瓶子可以自然消失，用户可以随时离开
5. **安静优先**：任何新功能都不能破坏页面的安静感

---

## 许可证

MIT