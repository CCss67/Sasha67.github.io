# Sasha · 英语口语陪练

一个纯前端、单文件、零依赖的英语口语对话陪练。

面向中文母语的成人英语学习者。以聊天方式推进，分四个阶段：**打招呼 → 前置测试 → 场景对话 → 收尾复盘**。

- 打开即用，无需注册，无需联网
- 支持文字输入和语音输入（Web Speech API）
- 支持英文朗读（慢速 + 常速各一遍）
- 单文件、离线可用、可部署到 GitHub Pages

---

## 目录

- [这是什么](#这是什么)
- [为什么做这个](#为什么做这个)
- [快速开始](#快速开始)
- [四个阶段](#四个阶段)
- [对话规则](#对话规则)
- [记忆系统](#记忆系统)
- [语音功能](#语音功能)
- [技术实现](#技术实现)
- [如何自定义](#如何自定义)
- [部署到 GitHub Pages](#部署到-github-pages)
- [常见问题](#常见问题)
- [已知限制](#已知限制)
- [后续规划](#后续规划)
- [许可证](#许可证)

---

## 这是什么

**Sasha** 是一个陪中文母语成人练英语口语的对话伙伴。

它不是老师，不是测评工具，是一个"愿意陪你慢慢说"的人。用户打开页面后，Sasha 会先打招呼、问名字，然后通过几轮轻松的对话摸清用户的英语水平，再根据水平给出匹配的练习。

整个流程分四个阶段：

1. **打招呼** —— 自我介绍 + 问名字
2. **前置测试** —— 3 个环节，摸清水平
3. **场景对话** —— 6~8 轮追问，根据照片或文字场景
4. **收尾复盘** —— 列出今天练过的句子 + 下次重点

---

## 为什么做这个

### 起点：成人学英语口语最大的障碍是"不敢开口"

和几十位中文母语的成人英语学习者聊过后，反复听到同一句话：

> "我知道该怎么说，但就是说不出来。"
> "我怕说错，怕被笑话。"
> "我不知道跟谁练，找外教太贵，找朋友不好意思。"

他们的痛点不是"不会语法"，而是**缺少一个不会评判、不会催促、可以随时停下来重来的练习对象**。

### 产品假设

如果一个 AI 能做到这三点，就能让用户愿意开口：

1. **永远不评判** —— 不纠正所有错误，只挑最关键的一个
2. **永远不催促** —— 每轮只问一个问题，卡住就给台阶
3. **永远给选项** —— 用户不需要从零组织语言，选一个就行

### 与市面上其他口语产品的差异

| 常见口语 App | Sasha |
|--------------|-------|
| 打分、评级、排行榜 | 不打分、不排名 |
| 纠正所有错误 | 每轮只纠 1 个 |
| 用标准答案评判 | 给选项，给台阶 |
| 要求用户主动发起 | 主动引导，降低门槛 |
| 强调"进步" | 强调"敢开口" |

---

## 快速开始

1. 保存代码为 `index.html`
2. 双击用浏览器打开（推荐 Chrome / Edge / Safari）
3. 看到 Sasha 的自我介绍
4. 输入名字，或选一个选项
5. 跟着对话一步步进行

**建议**：用 Chrome 打开，可以体验完整的语音输入 + 朗读功能。

---

## 四个阶段

### S0 · 打招呼 + 姓名互通

- Sasha 自我介绍：「Hi! 我是 Sasha，陪你练英语口语的伙伴」
- 问用户名字，给 3 个选项：
  - A. My name is... 我叫...
  - B. I'm... 我是...
  - C. 其他（中文说也行）
- 用户回答后，用名字称呼一次，确认收到

**目的**：建立关系，让用户感到"对方记住了我"。

### S1 · 前置测试（3 个环节）

#### S1-1 翻译测试

> 来，试一句翻译
> 我想买一杯咖啡。
> 用英语怎么说？

- 答对 → 肯定 + 进入下一环节
- 答错但能懂 → 先肯定 + 只挑 1 个最关键问题 + 带读正确句 + Repeat
- 不会 / 说中文 → 翻译 + 带读 + Repeat
- **不超过 2 轮**，测试目的是摸水平，不是教学

#### S1-2 自我介绍

> 现在，用英语介绍一下你自己吧
> 说 1~2 句就行。
> 关键词：name / job / hobby / city

根据用户的回答，判定水平：

| 表现 | 判定 |
|------|------|
| 只能说单词 | 起步阶段 |
| 能说短句 | 基础阶段 |
| 能说 2 句以上 | 进阶阶段 |

#### S1-3 开放问题

> 你为什么想练英语？
> Why do you want to practice English?

给 4 个选项：
- A. 为了旅行。For travel.
- B. 为了工作。For work.
- C. 为了交朋友。To make friends.
- D. 其他（中文说也行）

### S2 · 评估 + 生成个性化档案

根据 S1 三个环节的表现，给出阶段判定：

**起步阶段**
> 我看了一下，你现在在**起步阶段**
> 完全正常，我们从最简单的句子开始，一步一步来。
> 后面我会多给你选项，多带你读，不着急。

**基础阶段**
> 你现在在**基础阶段**
> 已经能开口说了，很棒！
> 后面我们练完整一点的句子，慢慢加长。

**进阶阶段**
> 你现在在**进阶阶段**
> 表达挺流畅的！
> 后面我们少给选项，多聊开放话题。

**关键**：评估结果用一句话说完，不展开分析，不打分，不列错误。

### S3 · 场景对话（6~8 轮）

#### 场景识别

用户上传照片或直接打字告诉场景：

| 场景 | 类型 |
|------|------|
| 咖啡店 / 餐厅 | 点单场景 |
| 商场 / 商店 | 购物场景 |
| 街道 / 路口 | 问路场景 |
| 酒店 / 机场 | 入住 / 值机场景 |
| 其他 | 通用闲聊场景 |

#### 对话流程

每轮先给一个问题 + 2~3 个选项。用户答完后，根据上一轮回答**灵活追问**：

- Q1：What are you doing?
- Q2：具体细节 —— 你具体在做什么？
- Q3：频率 / 习惯 —— 你常这样做吗？
- Q4：和谁 —— 你一个人还是和别人一起？
- Q5：感觉 —— 你感觉怎么样？
- Q6：原因 —— 为什么？
- Q7：展开 —— 再多说一点？

**难度动态调整**：

| 阶段 | 目标句长 |
|------|----------|
| 起步 | 3~5 个词 |
| 基础 | 6~10 个词 |
| 进阶 | 10+ 个词 |

**轮数上限 8 轮**，到了或用户明显想结束就收尾。

### S4 · 收尾复盘

> 今天练了这几句：[列出用户真正说过 / 跟读过的句子]
> 你现在的阶段：[起步 / 基础 / 进阶]
> 你需要注意的地方：[累计 3 次以上的错误类型，最多 2 条]
> 下次重点：[根据错误类型给出建议]
> 下次我们继续

只列用户真正说过的句子，不列没选的选项。

---

## 对话规则

### 全局原则

1. **你是在聊天，不是在上课**。语气轻松、鼓励、有耐心。
2. **每轮只问一个问题**。
3. **问问题时给 2~3 个选项**，每个选项 5 个词以内，必须带中文意思。
4. **用户选完后，追问方向必须跟选项走**。
5. **用户自己说 → 跟着用户的话走**，选项作废。
6. **用户卡住 → 给更短的台阶**（先 Yes/No），再补完整句。
7. **核心**：用户说"我不会"、"I can't speak"或直接说中文 → 翻译成英文 + 引导跟读 + 再追问。
8. **一次只挑 1 个发音问题**，不催促、不评价。
9. **支持语音输入**：用户的语音转文字后，按同样规则处理。

### 语言规则

中文在前、英文在后，一一对应。

例：这个多少钱？How much is this?

### 音频规则

英文目标句读两遍：慢速一遍 + 常速一遍。

### 特殊处理：听不懂 / 说不出 / 直接说中文

1. 先安抚：没关系，这是正常的！
2. 解释上一轮关键词的中文意思
3. 拆小：先 Yes/No → 再二选一 → 最后开放
4. 用更简单的中文重说
5. 重复原来的英文问题
6. **核心**：当用户明确表示"我不会"、"I can't speak"或直接用中文回答时：
   - 先鼓励："没关系，我们从这句开始学"
   - 将用户想表达的中文翻译成最简单的英文
   - 带读这句英文，并给出：Repeat: [英文句子]
   - 等用户跟读完成后（或表示跳过），立刻回到刚才的对话情境，继续追问

### 救命句（用户随时可要求）

- 请说慢一点。Can you speak more slowly, please?
- 请再说一遍。Can you say that again, please?
- 我没听懂。I didn't understand.
- 这个用英语怎么说？How do you say this in English?
- 请写在纸上。Can you write it down, please?
- 我需要帮助。I need help.

---

## 记忆系统

每轮对话结束时，更新以下**短期记忆槽位**：

| 槽位 | 说明 |
|------|------|
| 姓名 | 用户名字 |
| 水平 | 起步 / 基础 / 进阶 |
| 学习目标 | 旅行 / 工作 / 交朋友 / 其他 |
| 本轮已练句子 | 只记录用户真正说过 / 跟读过的句子 |
| 常见错误类型 | 冠词缺失 / 时态混乱 / 单复数错误 / 发音问题等 |
| 本轮对话轮数 | 计数 |

### 错误日志规则

- 用户每犯一个错误，记录到「常见错误类型」槽位
- 同一类错误出现 **3 次以上**，在 S4 复盘时重点提示
- 每次纠错只挑 **1 个**最关键问题，不一次纠正多个

### 长对话防遗忘机制

当对话超过 6 轮时，在每轮回复前隐式执行：

1. 压缩早期对话：把 S0-S2 的关键信息总结成 1 句话摘要
2. 保留最近 3 轮原始对话
3. 显式标记：【当前阶段：S3 场景对话】【用户姓名：XX】【用户水平：XX】
4. 关键信息（姓名、学习目标）用【背景】标签做显式锚定

---

## 语音功能

### 语音输入

- 使用浏览器内置的 **Web Speech API**
- 点击麦克风按钮开始录音，说完后自动识别为文字
- 识别语言为 `en-US`（美式英语）
- 识别结果自动填入输入框并发送

### 语音朗读

- 使用浏览器内置的 **SpeechSynthesis API**
- 英文目标句自动朗读，慢速一遍 + 常速一遍
- 慢速：`rate = 0.75`
- 常速：`rate = 1.0`

### 兼容性

| 浏览器 | 语音输入 | 语音朗读 |
|--------|----------|----------|
| Chrome | ✅ | ✅ |
| Edge | ✅ | ✅ |
| Safari | ✅ | ✅ |
| Firefox | ❌ | ✅ |

不支持语音输入的浏览器会自动隐藏麦克风按钮。

---

## 技术实现

### 技术栈

- 纯 HTML + CSS + 原生 JavaScript
- 单文件，零依赖
- 无后端，无数据收集
- 数据存在内存里，刷新即重置

### 状态机设计

整个对话是一个状态机，分为 5 个状态：

```js
let stage = 'S0';    // S0 / S1 / S2 / S3 / S4
let subStep = 0;     // 每个阶段内部的子步骤
```

### 核心数据结构

```js
const memory = {
  name: null,          // 姓名
  level: null,         // 起步 / 基础 / 进阶
  goal: null,          // 旅行 / 工作 / 交朋友 / 其他
  practiced: [],       // 用户真正说过/跟读过的句子
  errors: {},          // { 错误类型: 次数 }
  rounds: 0            // 对话轮数
};
```

### 场景数据

```js
const SCENES = {
  '咖啡店': '点单', '餐厅': '点单',
  '商场': '购物', '商店': '购物',
  '街': '问路', '路口': '问路',
  '酒店': '入住', '机场': '值机'
};

const QFLOW = [
  { q: 'What are you doing?', opts: [...] },
  { q: 'What exactly are you doing?', opts: [...] },
  // ... 共 7 轮
];
```

### 中文翻译

当前是一个简单的关键词映射：

```js
function translateSimple(zh) {
  const map = {
    '我不会': "I can't speak English.",
    '我不会说': "I can't speak English.",
    '我不知道': "I don't know.",
    '我想买一杯咖啡': "I'd like a coffee.",
    '这个多少钱': "How much is this?",
    // ...
  };
  for (const k in map) if (zh.includes(k)) return map[k];
  return "Let me try. " + zh;
}
```

**这是 v1 的简化实现，后续可替换为大模型 API**（见「后续规划」）。

### 核心流程

```
启动
 ↓
stepS0()  打招呼 + 问名字
 ↓
stepS1()  翻译测试 + 自我介绍 + 开放问题
 ↓
stepS2()  判定水平
 ↓
stepS3()  场景对话（循环 7 轮）
 ↓
stepS4()  收尾复盘
```

每个 `step` 函数接收用户输入，更新状态，输出下一轮消息。

---

## 如何自定义

### 修改 Sasha 的人设

在 `stepS0()` 里修改自我介绍：

```js
addMsg(`Hi! 我是 Sasha，陪你练英语口语的伙伴<br>
  <span class="en">I'm Sasha. Nice to meet you!</span>`);
```

### 修改场景对话的问题流

在 `QFLOW` 数组里增删或修改：

```js
const QFLOW = [
  { q: '你的新问题？', opts: [
    { label: 'A. 选项一  中文', value: 'English option 1.' },
    // ...
  ]},
];
```

### 修改场景识别词

在 `SCENES` 对象里增删：

```js
const SCENES = {
  '咖啡店': '点单',
  '书店': '购书',    // 新增
  // ...
};
```

### 修改中文翻译映射

在 `translateSimple()` 的 `map` 里增删：

```js
const map = {
  '我不会': "I can't speak English.",
  // 新增你需要的短语
};
```

### 修改主题色

主色是蓝色渐变 `#6a8dff → #9b6aff`，全局替换即可：

```bash
# macOS
sed -i '' 's/#6a8dff/#10b981/g' index.html
sed -i '' 's/#9b6aff/#059669/g' index.html
```

---

## 部署到 GitHub Pages

### 1. 文件准备

```
sasha-english/
├── index.html       # 把代码保存为 index.html
└── README.md
```

### 2. 上传到 GitHub

1. 新建仓库 `sasha-english`（Public）
2. `Add file` → `Upload files` → 拖入所有文件
3. `Commit changes`

### 3. 开启 Pages

1. 仓库 → `Settings` → `Pages`
2. Source 选 `Deploy from a branch`
3. Branch 选 `main`，目录选 `/ (root)`
4. 点 `Save`，等 1~2 分钟
5. 访问 `https://你的用户名.github.io/sasha-english/`

### 4. 后续更新

```bash
git add .
git commit -m "update"
git push
```

Pages 会自动重新部署。

---

## 常见问题

### Q1：为什么 Sasha 记不住我上次说了什么

当前版本**不保存任何数据**，所有对话记录只存在内存里，刷新页面即重置。

如需保存历史，见「后续规划」中的 P1 项。

### Q2：麦克风按钮点了没反应

部分浏览器（如 Firefox）不支持 Web Speech API。
页面会自动隐藏麦克风按钮，可以改用文字输入。

### Q3：为什么没有声音

部分浏览器（如 Safari）需要用户先交互一次才允许播放音频。
点一下页面任意位置，再试一次。

### Q4：中文输入为什么也会被处理

Sasha 会自动识别中文输入，并翻译成最简单的英文让用户跟读。
这是"用户卡住 → 给台阶"的设计，不是 bug。

### Q5：怎么修改 Sasha 的名字

在 `stepS0()` 里全局替换 "Sasha" 即可。

### Q6：能不能接入大模型 API

可以。当前 `translateSimple()` 是简单的关键词映射，替换成 API 调用即可：

```js
async function translateSimple(zh) {
  const res = await fetch('https://your-api.com/translate', {
    method: 'POST',
    body: JSON.stringify({ text: zh, target: 'en' })
  });
  return (await res.json()).translation;
}
```

详见「后续规划」。

---

## 已知限制

### v1（当前版本）的限制

- **中文翻译是关键词映射，不是真正理解**
- **场景对话是固定问题流，不会根据上下文动态生成**
- **不保存任何历史记录**（刷新即重置）
- **水平判定是简单的词数 / 句数统计**
- **没有错误日志的持久化**（只在当前对话里累计）
- **没有长对话防遗忘机制**（上下文超过一定长度后会丢失）
- **没有 TTS 音色选择**
- **没有多语言支持**

### 这些限制的设计取舍

v1 有意选择"简单实现"，因为：

1. **先验证产品形态**：先确认"分阶段引导 + 选项 + 不评判"这个模式有效
2. **先做最小可用**：没有 API Key 也能玩，降低使用门槛
3. **先跑通全流程**：S0 到 S4 的完整体验，比某一环节精致更重要

v2 会用 LLM 替换硬编码逻辑，见下。

---

## 后续规划

### v2 · 接入 LLM

**目标**：从"固定流程脚本"升级为"真正理解用户的对话伙伴"。

**核心变化**：

| 模块 | v1 | v2 |
|------|-----|-----|
| 中文翻译 | 关键词映射 | LLM 实时翻译 |
| 场景对话 | 固定 7 轮 | 根据用户回答动态追问 |
| 水平判定 | 词数 / 句数统计 | LLM 综合评估 |
| 纠错 | 无 | 实时识别 + 每轮只挑 1 个 |
| 错误日志 | 无 | 持久化 + 累计 3 次提示 |
| 复盘 | 固定模板 | 根据对话内容生成个性化建议 |

**技术路径**：

```js
async function nextStepWithLLM(conversation, memory) {
  const prompt = `
    你是一位英语口语陪练 Sasha。
    当前阶段：${stage}
    用户水平：${memory.level}
    已练句子：${memory.practiced.join(', ')}
    错误累计：${JSON.stringify(memory.errors)}
    
    对话历史：
    ${conversation.map(m => `${m.role}: ${m.text}`).join('\n')}
    
    请生成下一轮回复。
    要求：
    1. 只问一个问题
    2. 给 2~3 个选项（每个 5 个词以内，带中文意思）
    3. 一次只纠 1 个错
    4. 语气轻松、鼓励、有耐心
  `;
  return await callLLM(prompt);
}
```

**优先级**：

| 优先级 | 功能 | 说明 |
|--------|------|------|
| P0 | 接入 LLM API | 替换 `translateSimple()` |
| P0 | 动态场景追问 | 替换固定 QFLOW |
| P1 | 水平判定升级 | 用 LLM 综合评估 |
| P1 | 错误日志持久化 | 存 localStorage |
| P1 | 复盘个性化 | 根据对话生成建议 |
| P2 | 历史记录 | 保存所有对话 |
| P2 | 多语言支持 | 不只是中文母语者 |
| P2 | TTS 音色选择 | 多种声音可选 |
| P3 | 用户系统 | 跨设备同步 |
| P3 | 导出对话 | 复制 / 下载 txt |

### v2 · 设计原则（延续 v1）

无论 v2 加入多少 AI 能力，以下原则不变：

1. **不评判** —— 不纠所有错，只挑最关键的一个
2. **不催促** —— 每轮只问一个问题，卡住就给台阶
3. **永远给选项** —— 用户不需要从零组织语言
4. **轻松优先** —— 像聊天，不像上课
5. **本地优先** —— 默认不上传任何数据

### v3 · 长期方向

- 语音对话（实时语音输入输出，不用打字）
- 情境模拟（不只场景对话，还有角色扮演）
- 社群（用户之间互相练）
- 个性化学习路径（根据长期数据推荐练习内容）

---

## 许可证

MIT